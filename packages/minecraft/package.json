{
  "name": "@minechain/minecraft",
  "version": "0.0.0",
  "scripts": {
    "package": "mvn clean package assembly:single",
    "build": "npm run package",
    "clean": "mvn clean",
    "test": "mvn test"
  }
}
