// SPDX-License-Identifier: MIT
pragma solidity ^0.8.18;

contract Minechain {
    // numberOfTokens = 1024
    
    // mapping(address => Account) public accounts;

    // tokenHolderOnly, ValidTokenOnly
    // setPriceOf(tokenid, price)

    // validTokenOnly, nonTokenHolderOnly
    // buy(tokenId, newPrice)

    // depositRent()
    // withdrawRent()

    // ownerOnly
    // collectRent(tokenId)
}
